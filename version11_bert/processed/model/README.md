# Model artefacts

The trained `xlm-roberta-base` weights are intentionally not committed to
GitHub because `model.safetensors` is about 1.1 GB.

Download the Drive artefact bundle and place the files in this directory before
running live inference.
